spring.application.name=demo

# Don't change the port as per test environment
server.port=9001

# Database Configuration
# Changed database name to integrity_db as per your project screenshots
spring.datasource.url=jdbc:mysql://localhost:3306/integrity_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Security / JWT Settings 
# REQUIRED: The JwtTokenProvider will fail to initialize without these
app.jwtSecret=AcademicIntegrityCaseTrackerSecretKeyThatIsLongEnoughToPassHS512Validation12345
app.jwtExpirationMs=86400000

# Servlet / Forwarding
server.forward-headers-strategy=framework

# Swagger/OpenAPI (Step 6 requirement)
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs